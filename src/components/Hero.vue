<template>
  <section id="home" class="hero">
    <div class="hero-container">
      <!-- Profile Image Section -->
      <div class="profile-section">
        <div class="profile-image-wrapper">
          <div class="profile-frame">
            <img src="/images/profilephoto.jpeg" alt="Colin Owen Mutwiri Nyaga" class="profile-image" />
            <div class="profile-glow"></div>
            <div class="profile-ring"></div>
          </div>
          <div class="floating-badge">
            <span class="badge-icon">✨</span>
            <span class="badge-text">Available for Collaborations</span>
          </div>
        </div>
      </div>

      <!-- Content Section -->
      <div class="hero-content">
        <div class="intro-badge">
          <span class="badge-dot"></span>
          Full-Stack Developer & Tech Innovator
        </div>
        
        <h1 class="hero-title">Colin Owen Mutwiri Nyaga</h1>
        
        <p class="hero-subtitle">
          Building Beautiful, Scalable Solutions
        </p>
        
        <p class="hero-description">
          CS graduate from Catholic University of Eastern Kenya with expertise in web development, mobile apps, cybersecurity, and AI/ML. Transforming ideas into elegant digital experiences.
        </p>

        <div class="hero-stats">
          <div class="stat-item">
            <span class="stat-value">15+</span>
            <span class="stat-name">Projects</span>
          </div>
          <div class="stat-item">
            <span class="stat-value">4+</span>
            <span class="stat-name">Years</span>
          </div>
          <div class="stat-item">
            <span class="stat-value">8</span>
            <span class="stat-name">Tech Stacks</span>
          </div>
        </div>

        <div class="hero-cta">
          <a href="#collaboration" class="btn btn-primary">Let's Collaborate</a>
          <a href="#projects" class="btn btn-secondary">View My Work</a>
        </div>

        <div class="social-links">
          <a href="https://github.com/owenz4040" target="_blank" rel="noopener" class="social-icon" title="GitHub">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.44 9.8 8.21 11.39.6.11.79-.26.79-.58v-2.23c-3.34.73-4.04-1.42-4.04-1.42-.55-1.39-1.33-1.76-1.33-1.76-1.09-.75.08-.73.08-.73 1.21.08 1.84 1.24 1.84 1.24 1.07 1.83 2.81 1.3 3.49 1 .11-.78.42-1.31.76-1.61-2.67-.3-5.47-1.33-5.47-5.93 0-1.31.47-2.38 1.24-3.22-.12-.3-.54-1.52.12-3.18 0 0 1.01-.32 3.3 1.23.96-.27 1.98-.4 3-.41 1.02.01 2.04.14 3 .41 2.29-1.55 3.29-1.23 3.29-1.23.66 1.66.24 2.88.12 3.18.77.84 1.24 1.91 1.24 3.22 0 4.61-2.81 5.63-5.48 5.92.43.37.82 1.1.82 2.22v 3.29c0 .32.19.69.8.58C20.56 21.8 24 17.3 24 12c0-6.63-5.37-12-12-12z"/>
            </svg>
          </a>
          <a href="https://linkedin.com/in/OwenCmn" target="_blank" rel="noopener" class="social-icon" title="LinkedIn">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M20.45 20.45h-3.55v-5.57c0-1.33-.48-2.24-1.99-2.24-1.08 0-1.72.73-2 1.44-.1.25-.13 1-.13 1.76v4.6h-3.55V9.9h3.55v1.27h.05c.48-.9 1.64-1.85 3.37-1.85 3.6 0 4.27 2.37 4.27 5.46v4.42zM5.34 9.43c-1.14 0-2.06-.93-2.06-2.07 0-1.14.92-2.06 2.06-2.06 1.14 0 2.06.92 2.06 2.06 0 1.14-.92 2.07-2.06 2.07zm1.78 10.02H3.56V9.91h3.56v9.54zM22.23 0H1.77C.79 0 0 .77 0 1.73v20.54C0 23.23.79 24 1.77 24h20.45c.98 0 1.77-.77 1.77-1.73V1.73c0-.96-.79-1.73-1.77-1.73z"/>
            </svg>
          </a>
          <a href="https://twitter.com/CmnOwen" target="_blank" rel="noopener" class="social-icon" title="Twitter">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M23.95 4.57c.88-.51 1.56-1.32 1.88-2.29-.82.49-1.73.84-2.68 1.03-.77-.82-1.87-1.33-3.08-1.33-2.33 0-4.22 1.89-4.22 4.22 0 .33.04.66.13.98-3.51-.18-6.62-1.86-8.7-4.41-.36.62-.57 1.34-.57 2.1 0 1.47.75 2.76 1.88 3.52-.7-.02-1.37-.21-1.95-.52v.05c0 2.05 1.46 3.76 3.4 4.15-.35.1-.73.15-1.11.15-.27 0-.54-.03-.8-.08.54 1.67 2.1 2.88 3.94 2.91-1.45 1.14-3.28 1.81-5.27 1.81-.34 0-.68-.02-1.02-.06 1.88 1.21 4.12 1.91 6.52 1.91 7.83 0 12.11-6.48 12.11-12.11 0-.18 0-.37-.01-.56.83-.6 1.55-1.35 2.11-2.21z"/>
            </svg>
          </a>
          <a href="mailto:owenzcolin@gmail.com" class="social-icon" title="Email">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="4" width="20" height="16" rx="2"/>
              <path d="M22 6l-10 5-10-5"/>
            </svg>
          </a>
        </div>
      </div>
    </div>

    <div class="scroll-indicator">
      <span>Scroll to explore</span>
      <div class="scroll-arrow">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M10 3v10M16 13l-6 6-6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
</script>

<style scoped>
.hero {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 2rem;
  position: relative;
  overflow: hidden;
  background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 50%, #0a0a0a 100%);
}

.hero::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 1000px;
  height: 1000px;
  background: radial-gradient(circle, rgba(224, 64, 95, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  animation: float 20s ease-in-out infinite;
  z-index: 0;
}

.hero::after {
  content: '';
  position: absolute;
  bottom: -50%;
  left: -50%;
  width: 1000px;
  height: 1000px;
  background: radial-gradient(circle, rgba(131, 58, 180, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  animation: float-reverse 20s ease-in-out infinite;
  z-index: 0;
}

@keyframes float {
  0%, 100% { transform: translateY(0px) translateX(0px); }
  50% { transform: translateY(-50px) translateX(50px); }
}

@keyframes float-reverse {
  0%, 100% { transform: translateY(0px) translateX(0px); }
  50% { transform: translateY(50px) translateX(-50px); }
}

.hero-container {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  position: relative;
  z-index: 2;
}

/* Profile Section */
.profile-section {
  display: flex;
  justify-content: center;
  align-items: center;
}

.profile-image-wrapper {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rem;
}

.profile-frame {
  position: relative;
  width: 380px;
  height: 380px;
  border-radius: 50%;
  overflow: hidden;
  z-index: 3;
}

.profile-glow {
  position: absolute;
  inset: -10px;
  background: var(--primary-gradient);
  border-radius: 50%;
  opacity: 0.6;
  blur: 20px;
  z-index: -1;
  animation: pulse-glow 3s ease-in-out infinite;
}

.profile-ring {
  position: absolute;
  inset: -5px;
  border: 3px solid;
  border-image: var(--primary-gradient) 1;
  border-radius: 50%;
  animation: spin 20s linear infinite;
}

@keyframes pulse-glow {
  0%, 100% { opacity: 0.5; filter: blur(20px); }
  50% { opacity: 0.8; filter: blur(30px); }
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.profile-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 50%;
  display: block;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
}

.floating-badge {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  background: linear-gradient(135deg, rgba(224, 64, 95, 0.2) 0%, rgba(131, 58, 180, 0.2) 100%);
  border: 1px solid rgba(224, 64, 95, 0.4);
  padding: 0.8rem 1.5rem;
  border-radius: 50px;
  backdrop-filter: blur(10px);
  animation: bounce 3s ease-in-out infinite;
}

@keyframes bounce {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

.badge-icon {
  font-size: 1.2rem;
}

.badge-text {
  font-size: 0.9rem;
  font-weight: 600;
  background: var(--primary-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Content Section */
.hero-content {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  color: var(--text-primary);
}

.intro-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  background: linear-gradient(135deg, rgba(64, 93, 230, 0.1) 0%, rgba(224, 64, 95, 0.1) 100%);
  border: 1px solid rgba(224, 64, 95, 0.3);
  border-radius: 50px;
  width: fit-content;
  font-size: 0.9rem;
  font-weight: 600;
  color: #e4405f;
}

.badge-dot {
  width: 8px;
  height: 8px;
  background: linear-gradient(135deg, #e4405f 0%, #fd1d1d 100%);
  border-radius: 50%;
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(1.2); }
}

.hero-title {
  font-size: 3.5rem;
  font-weight: 900;
  line-height: 1.2;
  background: var(--primary-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0;
}

.hero-subtitle {
  font-size: 1.5rem;
  color: var(--text-secondary);
  margin: 0;
  font-weight: 600;
}

.hero-description {
  font-size: 1.1rem;
  line-height: 1.8;
  color: var(--text-muted);
  max-width: 500px;
  margin: 1rem 0;
}

.hero-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
  margin: 2rem 0;
  padding: 1.5rem;
  background: linear-gradient(135deg, rgba(224, 64, 95, 0.05) 0%, rgba(131, 58, 180, 0.05) 100%);
  border: 1px solid rgba(224, 64, 95, 0.2);
  border-radius: 1rem;
}

.stat-item {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  text-align: center;
}

.stat-value {
  font-size: 2rem;
  font-weight: 900;
  background: var(--primary-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.stat-name {
  font-size: 0.9rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.hero-cta {
  display: flex;
  gap: 1.5rem;
  margin-top: 1rem;
}

.btn {
  padding: 1rem 2.5rem;
  border-radius: 50px;
  font-weight: 700;
  text-decoration: none;
  transition: all 0.3s ease;
  border: 2px solid;
  font-size: 1rem;
  cursor: pointer;
  text-transform: uppercase;
  letter-spacing: 1px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.btn-primary {
  background: var(--primary-gradient);
  border-color: #e4405f;
  color: white;
  box-shadow: 0 8px 24px rgba(224, 64, 95, 0.3);
}

.btn-primary:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 32px rgba(224, 64, 95, 0.5);
}

.btn-secondary {
  background: transparent;
  border-color: #e4405f;
  color: #e4405f;
}

.btn-secondary:hover {
  background: rgba(224, 64, 95, 0.1);
  transform: translateY(-4px);
}

.social-links {
  display: flex;
  gap: 1.2rem;
  margin-top: 2rem;
}

.social-icon {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(224, 64, 95, 0.1) 0%, rgba(131, 58, 180, 0.1) 100%);
  border: 1px solid rgba(224, 64, 95, 0.3);
  color: #e4405f;
  transition: all 0.3s ease;
  text-decoration: none;
}

.social-icon:hover {
  background: linear-gradient(135deg, rgba(224, 64, 95, 0.2) 0%, rgba(131, 58, 180, 0.2) 100%);
  border-color: rgba(224, 64, 95, 0.6);
  transform: translateY(-8px);
  box-shadow: 0 12px 24px rgba(224, 64, 95, 0.2);
}

.social-icon svg {
  width: 20px;
  height: 20px;
}

.scroll-indicator {
  position: absolute;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%);
  text-align: center;
  z-index: 2;
}

.scroll-indicator span {
  display: block;
  font-size: 0.9rem;
  color: var(--text-muted);
  margin-bottom: 0.5rem;
}

.scroll-arrow {
  display: flex;
  justify-content: center;
  animation: bounce-down 2s ease-in-out infinite;
}

@keyframes bounce-down {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(10px); }
}

/* Responsive */
@media (max-width: 1024px) {
  .hero-container {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .profile-frame {
    width: 300px;
    height: 300px;
  }

  .hero-title {
    font-size: 2.5rem;
  }

  .hero-subtitle {
    font-size: 1.2rem;
  }
}

@media (max-width: 768px) {
  .hero {
    padding: 2rem 1rem;
  }

  .hero-container {
    gap: 1.5rem;
  }

  .hero-title {
    font-size: 2rem;
  }

  .hero-subtitle {
    font-size: 1rem;
  }

  .hero-cta {
    flex-direction: column;
  }

  .btn {
    padding: 0.8rem 2rem;
    font-size: 0.9rem;
  }

  .profile-frame {
    width: 250px;
    height: 250px;
  }

  .hero-stats {
    grid-template-columns: 1fr;
  }
}
</style>
